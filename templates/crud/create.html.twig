{% extends "base.html.twig" %}
{% block body %}
    <div>
        <h1>
            Create new
        </h1>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-info mt-2 text-white">
                Submit
            </button>
        {{ form_end(form) }}
    </div>
{% endblock %}